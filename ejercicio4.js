const areOdd = (n, m) => {

//determino si dos numeros son pares, gracias a la cuenta de dos en dos, por lo tanto
// si el residuo de n y m al hacerlo por 2 = 0, determino que son pares y me retorna true, de lo contrario lo retorna false
    if(n%2=== 0 && m%2=== 0)
    return true;
    
    else{
    
    return false;  
      
    
    
    }
    
    
    };
    module.exports = areOdd;